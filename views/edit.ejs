<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <title></title>
    <link rel="stylesheet" href="/app.css" />
  </head>

  <body>
    <h1>Edit Trivia Questions Page</h1>
    <form action="/trivia/<%=trivia.id%>?_method=PUT" method="POST">
      <!--  NOTE: the form is pre-populated with values for the server-->
      Question:
      <input type="text" name="question" value="<%=trivia.question%>" /><br />
      Answer 1:
      <input type="text" name="answer1" value="<%=trivia.answer1%>" /><br />
      Answer 2:
      <input type="text" name="answer2" value="<%=trivia.answer1%>" /><br />
      Answer 3:
      <input type="text" name="answer3" value="<%=trivia.answer1%>" /><br />
      Answer 4:
      <input type="text" name="answer4" value="<%=trivia.answer1%>" /><br />
      Correct Answer:
      <input
        type="number"
        name="correctAnswer"
        value="<%=trivia.correctAnswer%>"
      /><br />
      <select name="category">
        <% for ( let i = 0; i < categories.length; i++ ) { let selected = ( i ==
        0 ) ? "selected" : ""; %>
        <option value="<%=categories[i].id%>" selected
          ><%=categories[i].name%>
        </option>
        <% } %>
      </select>
      <input type="submit" value="Submit Changes" />
    </form>
  </body>
</html>
